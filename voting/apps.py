from django.apps import AppConfig


class VotingConfig(AppConfig):
    name = 'voting'
# voting/apps.py

# from django.apps import AppConfig
# from django.db.models import BigAutoField

# class VotingConfig(AppConfig):
#     default_auto_field = BigAutoField  # configure the default auto field to be BigAutoField
#     name = 'voting'

